"""Support package for the calibration-ratio convergence study.

``run.py`` is the standalone driver/report generator (see ``README.md``);
``samples.py`` holds the deterministic conversation content. The live pytest
entry point that asserts convergence is ``tests/context/test_ratio_convergence.py``.
"""
